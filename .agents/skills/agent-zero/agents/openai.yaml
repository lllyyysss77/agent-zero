interface:
  display_name: "Agent Zero"
  short_description: "Repo-specific patterns and workflows for agent-zero"
  default_prompt: "Use the agent-zero repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true